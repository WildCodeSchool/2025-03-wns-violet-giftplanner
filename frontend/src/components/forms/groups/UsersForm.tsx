export default function UsersForm() {
    return (
        <div className="flex flex-col gap-4 px-20 m-auto">
            {/* Zone réservée pour une future gestion des utilisateurs / participants lors de la création */}
          </div>
    )
}